## Purpose
[Briefly describe the core objective and context of this capability.]

## ADDED Requirements

### Requirement: [Clear requirement statement]
[Describe requirement details using MUST / SHALL / MUST NOT keywords.]

#### Scenario: [Scenario name]
- **WHEN** [precondition]
- **THEN** [expected outcome]

## MODIFIED Requirements
<!-- Copy verbatim from base spec if editing an existing requirement -->

### Requirement: [Existing requirement statement]
[Describe what changed. Use MUST / SHALL / MUST NOT keywords.]

#### Scenario: [Scenario name]
- **WHEN** [precondition]
- **THEN** [expected outcome]

---

<!-- 
  OpenSpec Delta Section Rules:
  
  1. Every spec.md MUST contain at least one ADDED or MODIFIED section.
  2. ADDED sections define new requirements introduced by this change.
  3. MODIFIED sections define changes to existing requirements.
  4. Each requirement MUST have at least one Scenario with WHEN/THEN.
  5. Use MUST / SHALL / MUST NOT for binding requirements.
  6. Use SHOULD / MAY for non-binding guidance.
  7. Do NOT use ## REMOVED (handled differently by OpenSpec CLI).
  
  File path MUST be: specs/<capability>/spec.md
  Do NOT generate flat specs/<capability>.md files.
-->
